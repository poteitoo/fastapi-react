[tool.poetry]
name = "api"
version = "0.1.0"
description = ""
authors = ["poteitoo <1105tigery@gmail.com>"]
readme = "README.md"
packages = [{include = "schemas", from = "src"}]

[tool.poetry.dependencies]
python = "^3.8"
fastapi = {extras = ["all"], version = "^0.95.1"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
