{
  "name": "api",
  "version": "1.0.0",
  "scripts": {
    "dev": "poetry run uvicorn src.main:app --reload",
    "lint": "pnpm format && poetry run flake8 .",
    "format": "poetry run black . && poetry run isort ."
  }
}